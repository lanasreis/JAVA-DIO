package com.example.springbootREST;

public class Definicao {
    /*
        JACKSON
        -Responsável por efeturar a Serialização e Desserializaçaõ de nossos objetos.
        -ObjectMapper
        -@JsonProperty("id")
        -@JsonFormar(pattern = "yyyy-MM-dd'T'HH:mm:ss")
        -@JsonIgnore
        -@JsonGetter
        -@JsonSetter

        BINDER
        -Custom Data Binder é fazer um mapper de uma propriedade que o mapper não reconhece por padrão ex um enum
    */

}
